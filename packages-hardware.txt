# Hardware-Specific Packages (REFERENCE ONLY - AUTO-DETECTED!)
# ================================================================
# NOTE: Hardware drivers are now AUTOMATICALLY DETECTED by install.sh!
# This file is kept as a reference and for manual override only.
#
# The installation script will automatically:
# - Detect your CPU (AMD/Intel) and install the correct microcode
# - Detect your GPU(s) (NVIDIA/AMD/Intel) and install appropriate drivers
# - Handle hybrid graphics systems (e.g., Intel iGPU + NVIDIA dGPU)
# - Ask for your confirmation before installing
#
# You only need to edit this file if automatic detection fails or you want
# to manually override the automatic selection.

# === CPU Microcode (Auto-detected) ===
# The script automatically installs ONE of these based on CPU detection:
#amd-ucode      # AMD CPUs
#intel-ucode    # Intel CPUs

# === GPU Drivers (Auto-detected) ===
# The script automatically installs the appropriate packages for detected GPU(s).

# --- NVIDIA GPU Drivers (Auto-installed if NVIDIA GPU detected) ---
#nvidia-utils
#lib32-nvidia-utils
#opencl-nvidia
#lib32-opencl-nvidia
#libva-nvidia-driver
#nvidia-settings
#linux-cachyos-nvidia-open
#linux-cachyos-lts-nvidia-open

# --- AMD GPU Drivers (Auto-installed if AMD GPU detected) ---
#vulkan-radeon
#lib32-vulkan-radeon
#xf86-video-amdgpu

# --- Intel GPU Drivers (Auto-installed if Intel GPU detected) ---
#vulkan-intel
#lib32-vulkan-intel
#intel-media-driver
#libva-intel-driver

# === Manual Override ===
# If you need to manually override automatic detection:
# 1. Uncomment the packages you need above
# 2. Run: ./install.sh
#
# The script will respect any uncommented packages in this file.
#
# === Manual Installation ===
# To manually install without the script:
#   paru -S --needed $(grep -v '^#' packages-hardware.txt | grep -v '^$')
